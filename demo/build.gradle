group = 'com.epam'
version = '2.1'

repositories {
    mavenCentral()
}

dependencies {
    implementation project(':utils')
}

test {
    useJUnitPlatform()
}

compileJava {}.dependsOn(':utils:publishToMavenLocal')